{% extends 'base.html.twig' %}

{% block body %}
    <section>
        //todo: afficher les series
        {% for serie in series %}
        <article>
            <a href="#" title="View serie's details">
                <img src="{{ asset('img/posters/series') }}" alt="{{ serie.name }}">
            </a>
        </article>
        {% endfor %}
    </section>
{% endblock %}

{% block title %}
The best series | {{ parent() }}
{% endblock %}

